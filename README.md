https://macos-defaults.com/

Install homebrew

```shell
brew install mas
brew bundle
```

```shell
cp .zshrc ~/.zshrc
cp .gitconfig ~/.gitconfig
cp -r .config/* ~/.config
cp -r .local/bin/* ~/.local/bin
```
